[build]
  command = "npm run build"
  publish = "netlify-static"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "dist/netlify/functions"
  external_node_modules = ["playwright", "@sparticuz/chromium"]
  included_files = ["dist/tasks/**"]